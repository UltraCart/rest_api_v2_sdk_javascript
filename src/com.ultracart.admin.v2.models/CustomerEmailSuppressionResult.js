/**
 * UltraCart Rest API V2
 * UltraCart REST API Version 2
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: support@ultracart.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmailSuppressionSurfaces from './EmailSuppressionSurfaces';

/**
 * The CustomerEmailSuppressionResult model module.
 * @module com.ultracart.admin.v2.models/CustomerEmailSuppressionResult
 * @version 4.1.131
 */
class CustomerEmailSuppressionResult {
    /**
     * Constructs a new <code>CustomerEmailSuppressionResult</code>.
     * @alias module:com.ultracart.admin.v2.models/CustomerEmailSuppressionResult
     */
    constructor() { 
        
        CustomerEmailSuppressionResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CustomerEmailSuppressionResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:com.ultracart.admin.v2.models/CustomerEmailSuppressionResult} obj Optional instance to populate.
     * @return {module:com.ultracart.admin.v2.models/CustomerEmailSuppressionResult} The populated <code>CustomerEmailSuppressionResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomerEmailSuppressionResult();

            if (data.hasOwnProperty('bounce_cleared')) {
                obj['bounce_cleared'] = ApiClient.convertToType(data['bounce_cleared'], 'Boolean');
            }
            if (data.hasOwnProperty('customer_profile_oid')) {
                obj['customer_profile_oid'] = ApiClient.convertToType(data['customer_profile_oid'], 'Number');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('esp_suppression_removed')) {
                obj['esp_suppression_removed'] = ApiClient.convertToType(data['esp_suppression_removed'], 'Boolean');
            }
            if (data.hasOwnProperty('esp_warning')) {
                obj['esp_warning'] = ApiClient.convertToType(data['esp_warning'], 'String');
            }
            if (data.hasOwnProperty('global_unsubscribe_cleared')) {
                obj['global_unsubscribe_cleared'] = ApiClient.convertToType(data['global_unsubscribe_cleared'], 'Boolean');
            }
            if (data.hasOwnProperty('global_unsubscribed')) {
                obj['global_unsubscribed'] = ApiClient.convertToType(data['global_unsubscribed'], 'Boolean');
            }
            if (data.hasOwnProperty('global_unsubscribed_dts')) {
                obj['global_unsubscribed_dts'] = ApiClient.convertToType(data['global_unsubscribed_dts'], 'String');
            }
            if (data.hasOwnProperty('spam_complaint')) {
                obj['spam_complaint'] = ApiClient.convertToType(data['spam_complaint'], 'Boolean');
            }
            if (data.hasOwnProperty('spam_complaint_cleared')) {
                obj['spam_complaint_cleared'] = ApiClient.convertToType(data['spam_complaint_cleared'], 'Boolean');
            }
            if (data.hasOwnProperty('spam_complaint_dts')) {
                obj['spam_complaint_dts'] = ApiClient.convertToType(data['spam_complaint_dts'], 'String');
            }
            if (data.hasOwnProperty('suppression_surfaces')) {
                obj['suppression_surfaces'] = EmailSuppressionSurfaces.constructFromObject(data['suppression_surfaces']);
            }
        }
        return obj;
    }


}

/**
 * True only if this call actually removed bounce suppression.  Bounce has no per-customer flag, so there is no corresponding state field.
 * @member {Boolean} bounce_cleared
 */
CustomerEmailSuppressionResult.prototype['bounce_cleared'] = undefined;

/**
 * The customer profile oid that was operated on
 * @member {Number} customer_profile_oid
 */
CustomerEmailSuppressionResult.prototype['customer_profile_oid'] = undefined;

/**
 * The email address that was operated on
 * @member {String} email
 */
CustomerEmailSuppressionResult.prototype['email'] = undefined;

/**
 * True only if every requested suppression surface was successfully cleared.  When false, see esp_warning -- the profile was still modified.
 * @member {Boolean} esp_suppression_removed
 */
CustomerEmailSuppressionResult.prototype['esp_suppression_removed'] = undefined;

/**
 * Human readable explanation when esp_suppression_removed is false.  Suitable for display to a support agent.
 * @member {String} esp_warning
 */
CustomerEmailSuppressionResult.prototype['esp_warning'] = undefined;

/**
 * True only if this call actually changed the global unsubscribe flag.  False if it was already clear.
 * @member {Boolean} global_unsubscribe_cleared
 */
CustomerEmailSuppressionResult.prototype['global_unsubscribe_cleared'] = undefined;

/**
 * Global unsubscribe state AFTER this call
 * @member {Boolean} global_unsubscribed
 */
CustomerEmailSuppressionResult.prototype['global_unsubscribed'] = undefined;

/**
 * Global unsubscribe timestamp after this call.  Nulled when cleared; the prior value is retained on the audit record.
 * @member {String} global_unsubscribed_dts
 */
CustomerEmailSuppressionResult.prototype['global_unsubscribed_dts'] = undefined;

/**
 * Spam complaint state AFTER this call
 * @member {Boolean} spam_complaint
 */
CustomerEmailSuppressionResult.prototype['spam_complaint'] = undefined;

/**
 * True only if this call actually changed the spam complaint flag.  False if it was already clear.
 * @member {Boolean} spam_complaint_cleared
 */
CustomerEmailSuppressionResult.prototype['spam_complaint_cleared'] = undefined;

/**
 * Spam complaint timestamp after this call.  Nulled when cleared; the prior value is retained on the audit record.
 * @member {String} spam_complaint_dts
 */
CustomerEmailSuppressionResult.prototype['spam_complaint_dts'] = undefined;

/**
 * @member {module:com.ultracart.admin.v2.models/EmailSuppressionSurfaces} suppression_surfaces
 */
CustomerEmailSuppressionResult.prototype['suppression_surfaces'] = undefined;






export default CustomerEmailSuppressionResult;

