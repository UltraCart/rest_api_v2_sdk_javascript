/**
 * UltraCart Rest API V2
 * UltraCart REST API Version 2
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: support@ultracart.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SfvbItemContainer model module.
 * @module com.ultracart.admin.v2.models/SfvbItemContainer
 * @version 4.1.174
 */
class SfvbItemContainer {
    /**
     * Constructs a new <code>SfvbItemContainer</code>.
     * @alias module:com.ultracart.admin.v2.models/SfvbItemContainer
     */
    constructor() { 
        
        SfvbItemContainer.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SfvbItemContainer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:com.ultracart.admin.v2.models/SfvbItemContainer} obj Optional instance to populate.
     * @return {module:com.ultracart.admin.v2.models/SfvbItemContainer} The populated <code>SfvbItemContainer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SfvbItemContainer();

            if (data.hasOwnProperty('cjson_size')) {
                obj['cjson_size'] = ApiClient.convertToType(data['cjson_size'], 'Number');
            }
            if (data.hasOwnProperty('container_name')) {
                obj['container_name'] = ApiClient.convertToType(data['container_name'], 'String');
            }
            if (data.hasOwnProperty('hash_sha256')) {
                obj['hash_sha256'] = ApiClient.convertToType(data['hash_sha256'], 'String');
            }
            if (data.hasOwnProperty('last_modified')) {
                obj['last_modified'] = ApiClient.convertToType(data['last_modified'], 'String');
            }
            if (data.hasOwnProperty('merchant_item_id')) {
                obj['merchant_item_id'] = ApiClient.convertToType(data['merchant_item_id'], 'String');
            }
            if (data.hasOwnProperty('owner_object_id')) {
                obj['owner_object_id'] = ApiClient.convertToType(data['owner_object_id'], 'String');
            }
            if (data.hasOwnProperty('owner_type')) {
                obj['owner_type'] = ApiClient.convertToType(data['owner_type'], 'String');
            }
            if (data.hasOwnProperty('used_elements')) {
                obj['used_elements'] = ApiClient.convertToType(data['used_elements'], ['String']);
            }
        }
        return obj;
    }


}

/**
 * Size of the CJSON in UTF-8 bytes, which is the same unit the container version size and the upsell offer cjson_size use.
 * @member {Number} cjson_size
 */
SfvbItemContainer.prototype['cjson_size'] = undefined;

/**
 * The slot name.  This is the value an itemcontainer element's itemContainerName setting holds, which is what ties a container to the element that renders it.
 * @member {String} container_name
 */
SfvbItemContainer.prototype['container_name'] = undefined;

/**
 * SHA-256 of the container's CJSON.  Identical to the value a read of the container returns, so a listing is enough to start an If-Match write without reading first.
 * @member {String} hash_sha256
 */
SfvbItemContainer.prototype['hash_sha256'] = undefined;

/**
 * When the container was last written.  Absent for a container untouched since the storage began recording the time, so read an absent value as unknown rather than as never modified.
 * @member {String} last_modified
 */
SfvbItemContainer.prototype['last_modified'] = undefined;

/**
 * The owning item's merchant item id.  This is the identifier a storefront carries and a merchant recognizes; the oid appears nowhere on a rendered page.  Send it as owner_type itemid to address the same container without converting it first.
 * @member {String} merchant_item_id
 */
SfvbItemContainer.prototype['merchant_item_id'] = undefined;

/**
 * The merchant item oid, spelled the way containers/item/{owner_object_id} takes it.
 * @member {String} owner_object_id
 */
SfvbItemContainer.prototype['owner_object_id'] = undefined;

/**
 * Always item.  Carried so a row is usable as an address without the caller having to know which listing it came from.
 * @member {module:com.ultracart.admin.v2.models/SfvbItemContainer.OwnerTypeEnum} owner_type
 */
SfvbItemContainer.prototype['owner_type'] = undefined;

/**
 * The element types this container uses, recorded by the storage when it was written.  A large cjson_size beside a handful of element types is the signature of markup pasted into a single html element.  Absent on a container written before the storage began recording this.
 * @member {Array.<String>} used_elements
 */
SfvbItemContainer.prototype['used_elements'] = undefined;





/**
 * Allowed values for the <code>owner_type</code> property.
 * @enum {String}
 * @readonly
 */
SfvbItemContainer['OwnerTypeEnum'] = {

    /**
     * value: "item"
     * @const
     */
    "item": "item"
};



export default SfvbItemContainer;

